# Go Backend

We will build the explicit backend API in Go rather than TypeScript, despite the Expo React Native app using TypeScript. This trades away some shared-type convenience in favor of a backend stack the project owner is more excited to work in; API contracts and validation must therefore be treated as an explicit boundary between the mobile app and backend.
