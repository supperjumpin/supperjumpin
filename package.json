{
  "name": "supperjumpin",
  "private": true,
  "workspaces": [
    "apps/mobile",
    "packages/api-client"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "generate:api-client": "npm --workspace @supperjumpin/api-client run generate",
    "api:dev": "SUPPERJUMPIN_DEV_AUTH_TOKEN=dev-token go run ./apps/api/cmd/api",
    "api:test": "go test ./apps/api/..."
  }
}
